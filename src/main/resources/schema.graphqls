
schema {
	query: Query
	mutation: Mutation
}

type Query {}

type Mutation {}

type Info {
	# count of returned records
	count: Int
	
	# Total of records on database
	total: Int
	
	# Number of pages with the current size
	pages: Int
	
	# Current size
	size: Int
}